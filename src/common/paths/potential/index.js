const companyId = "?company_id=";
const collaborator = "collaborator";
const userIdQuery = "&user_id=";

export const NINEBOX_PROCESSES = {
  root: "/nine_boxes",
  mainSearch: companyId,
  collaborator,
  userIdQuery,
};

export const POTENTIAL_PROCESSES = {
  root: "/potential_processes",
  companyId,
};
