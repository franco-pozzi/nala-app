import styled from "styled-components";
import DialogActions from "@material-ui/core/DialogActions";

export const StyledInput = styled.div`
  margin: 7px 0 14px 0;
`;
export const StyledDialog = styled(DialogActions)`
  padding: 25px;
`;
